type Error {
  name: String!
  message: String!
  code: Int!
}

type User {
  id: ID!
  name: String!
}

type Query {
  getUsers(skip: Int, limit: Int): [User]
}

type Mutation {
  createUser(name: String): User
}
